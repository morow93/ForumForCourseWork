@model BeginApplication.Models.AvatarModel

@if (Model.isAvatarExists)
{
    <img src="@Url.Action("GetImage", "Account", new { userId = Model.UserId })"
         width="100" height="100" />
}
else
{
    <img src="~/Content/Images/avatar.jpg" width="100" height="100" />
}

@model List<BeginApplication.Models.ShortThemeInfo>

Тем : @Model.Count

@if (Model.Count != 0)
{ 
    foreach (var theme in Model)
    {
        <p>@Html.ActionLink(theme.ThemeTitle, "Theme", "Forum", new { id = theme.ThemeId}, null)</p>
    }
}
